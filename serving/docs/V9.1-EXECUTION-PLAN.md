# V9.1 Execution Plan

**Owner**: this conversation
**Created**: 2026-05-30
**Status**: DRAFT — awaiting user approval before execution

Five interlocking workstreams. Phases are ordered so each phase's results inform the next; some phases run in parallel where they don't conflict for the GPU.

---

## Phase 0 — Quick wins (DONE on this turn)
- ✅ `.gitignore`: ignore `serving/docker/wheels/`, `serving/docker/wheels.*/`, `*.whl`
- ✅ Profile reverted to working iter-3 (RedHat weights + NVIDIA Spark recipe), composer smoke 51/66 = 77.3%
- ✅ [V9-35B-A3B-NVFP4-NVIDIA-RECIPE.md](V9-35B-A3B-NVFP4-NVIDIA-RECIPE.md) updated with iter-4 verification, fail timing breakdown, vLLM PR #42124 finding, stack-side streaming gap

---

## Phase 1 — Direct lane conversation history

**Goal**: bring the direct lane to feature parity with the OpenClaw lane for multi-turn workflows, **without breaking the OpenClaw lane**. Both must remain functional and selectable per-request.

**Why**: direct lane is faster (no SSH-tunnel + sandbox indirection) but currently stateless — each `/api/assistant/chat` request is independent. Multi-turn smoke cases (the iter-32 chain-session work) fail or degrade when the prior turn's context isn't carried.

### Design (proposed — open to your input)

| Decision | Recommendation | Rationale |
|---|---|---|
| Where to hold history | **Direct-lane bridge** (`manyforge_assistant_bridge`, port 8100), in-memory per-process | Same pattern OpenClaw uses (gateway holds state). Bridge is the right boundary — it already mediates composer↔vLLM. Proxy stays transport-only. Composer doesn't need to change session-tracking logic. |
| Session identity | **`conversationId` from composer's existing payload** | Already sent for OpenClaw — no new composer change needed. Reuses the field. |
| Storage | **dict[session_id, deque[messages]]**, no persistence across bridge restarts | Matches OpenClaw which loses state on gateway restart. Simple, fast, no DB. |
| Eviction | **LRU by `last_use_ts`**, cap at N=50 active sessions; auto-evict after 30 min idle | Bounded memory. Smoke runs ~66 cases → max ~5 concurrent sessions in practice. |
| Context budget | **Truncate oldest turns when est_tokens > N** (where N defaults to 75% of model's max_model_len) | Simple and predictable. OpenClaw uses `/compact` (LLM summary); we can add that later if budget-truncate degrades quality. |
| Compatibility with OpenClaw lane | **Routing untouched** — composer keeps choosing lane via existing config (assistant provider = direct vs openclaw). Each lane manages its own state. | Both lanes always functional. No mutual interference. |
| Concurrency | **Per-session lock** so two requests for the same session can't race on the messages deque | Avoids interleaved history. |

### Implementation steps
1. **Read current direct-lane bridge**: `/home/tndlux/workspaces/dev_ws/src/manyforge/manyforge_assistant_bridge/bridge.py` (and supervisor.py) — map the request handler, the vLLM call site.
2. **Add `SessionHistory` class** with: `append(session_id, role, content)`, `materialize(session_id, system_messages, current_user_msg) → messages[]`, `evict_lru()`. Unit-testable in isolation.
3. **Wire into the request handler**: extract `conversationId` from incoming body → call `materialize()` → use returned messages array in the vLLM call. After response, append assistant turn (including reasoning if requested).
4. **Add a clear-session endpoint**: `POST /v1/sessions/{id}/clear` for the composer's "reset chat" button to call.
5. **Add Prometheus metrics**: `direct_lane_active_sessions`, `direct_lane_history_tokens{session_id}`, `direct_lane_eviction_total`.
6. **Smoke test parity**: run 3-prompt smoke chain (e.g., `PnP_01_scene_ground → PnP_02_scene_graspable → PnP_03_scene_pick_pad`) on both lanes, expect tool-call sequences to be equivalent.

### Success criteria
- Direct-lane smoke corpus ≥ 70% effective rate on multi-turn-heavy cases (currently fails most of them at 0%)
- OpenClaw lane smoke corpus unchanged (delta within ±2 pp of iter-3 baseline 51/66)
- No memory leak: bridge RSS stable across a 100-prompt run
- `/v1/sessions/{id}/clear` returns 204; subsequent prompt for same id behaves as turn 1

### Rollback
Single feature flag `DIRECT_LANE_HISTORY_ENABLED=true|false` env var. Default `true` once validated; set to `false` to fall back to stateless mode.

### Estimated effort
~3 hours implementation + 1 hour smoke validation.

---

## Phase 2 — Cosmos-reason2-8b regression hunt (both lanes)

**Goal**: identify and fix the v9 regression that dropped cosmos-reason2-8b from 51/66 (v8 baseline) to 34/66 (v9). Iterate autonomously while we have leads.

### Investigation plan

#### 2a — Inspect existing JSONL evidence (no GPU work, ~30 min)
We still have `/tmp/manyforge-assistant-e2e/vllm-proxy.jsonl.cosmos-baseline-185742` from the original 34/66 run. Before re-running:
- For each of the ~17 fail cases (34→51 delta), pull the actual XML emitted by the model from the JSONL response body
- Identify the specific mismatch: missing required field? wrong tool name? malformed XML? truncated emit?
- Categorize by failure type → drives the fix hypothesis tree

#### 2b — Run cosmos on both lanes (v9 image, current config)
- **OpenClaw lane**: reproduces 34/66 (sanity check) — if it does NOT reproduce, configuration drift has happened since the cosmos baseline run
- **Direct lane** (post Phase 1): clean comparison — if direct lane gets a different score, the regression is lane-specific (chain-session / template-application difference)

#### 2c — Bisect the regression
Based on findings from 2a/2b, run these in priority order until the fix is found:
1. **Chat template swap**: try froggeric template (currently used by 35B-A3B); if pass rate climbs, regression is template-driven
2. **Tool-call parser swap**: cosmos uses hermes by default; try qwen3_xml and qwen3_coder
3. **transformers downgrade**: pin to the version that v8 shipped (need to check VERSIONS.md for the exact v8 transformers pin); if pass rate recovers, transformers is the culprit
4. **vLLM downgrade probe**: if practical, install v0.20.x in a sidecar container to confirm v8-vs-v9 vLLM is the cause

#### 2d — Fix and re-baseline
Once root cause identified, apply fix and run full smoke on both lanes. Document recipe in `serving/docs/COSMOS-REASON2-8B-V9-RECIPE.md`.

### Success criteria
- Cosmos OpenClaw lane ≥ 51/66 (parity with v7/v8 baseline)
- Cosmos direct lane ≥ 49/66 (within 2 pp of OpenClaw)
- Root cause documented with evidence (JSONL diffs, version pins)

### Autonomy
You said "keep iterating autonomously" — I will do:
- All template/parser swaps
- All log inspection and fail-pattern analysis
- All smoke runs and result interpretation
I will **stop and ask** before:
- Downgrading vLLM (large blast radius)
- Modifying any composer Python code (composer is shared with OpenClaw lane work)

### Estimated effort
~6-8 hours including smoke runs.

---

## Phase 3 — v9.1 image: vLLM nightly with PR #42124

**Goal**: build a v9.1 image with vLLM main pinned to commit ≥ `6f5b533241` (PR #42124, "Add LM head quantization support for ModelOpt") to unblock NVIDIA NVFP4 weights for Qwen3.5/Qwen3.6.

### Steps
1. **Pick the SHA**: a commit on `main` from 2026-05-27 or later. Recommend pinning to a known-good day's HEAD rather than rolling nightly (reproducibility).
2. **Update `serving/docker/Dockerfile.vllm`**: change `VLLM_GIT_SHA` (or equivalent var) to the chosen commit. Bump `VLLM_VERSION` label to `0.22.1.dev<sha>`.
3. **Test build locally**: ~45 min wall-clock for full vLLM compile on Thor (Triton, FlashInfer, custom ops, CUTLASS DSL).
4. **Smoke-test v9.1 against the iter-3 working profile** (RedHat 35B-A3B-NVFP4) — must still boot and serve. Run a quick P1 probe.
5. **Tag the image**: `nemoclaw-thor/vllm:v0.22-pr42124-thor-sm110-cu132-v9.1` (mirror v9 naming).
6. **Document delta** in `serving/docs/V9.1-IMAGE-NOTES.md`: what changed vs v9, what's expected to unblock.

### Success criteria
- Image builds without errors
- `qwen3.6-35b-a3b-nvfp4-nvidia` profile (RedHat weights) still boots on v9.1 — smoke probe P1 passes
- No regression on any other profile that was healthy on v9

### Rollback
v9 image remains tagged and usable. v9.1 is additive.

### Estimated effort
~2 hours build + 1 hour smoke regression.

---

## Phase 4 — Test NVIDIA's 35B-A3B-NVFP4 on v9.1

**Goal**: verify whether PR #42124 actually closes the `lm_head.input_scale` gap, then run the full smoke corpus on NVIDIA's weights.

**Depends on**: Phase 3 complete.

### Steps
1. **Switch profile to NVIDIA weights** (one-line in `launch.sh`): `THOR_LAUNCH_MODEL_SOURCE="nvidia/Qwen3.6-35B-A3B-NVFP4"`
2. **Boot test**: weight load must pass 67% mark without `lm_head.input_scale` ValueError. Capture the engine init log to confirm MoE backend choice (expect MARLIN).
3. **Quick chat probe**: 512-token reply, verify no NaN/garbage in output. Throughput should be in the same ballpark as RedHat (~19 tok/s).
4. **Full 66-case smoke corpus** with iter-32 proxy/bridge caps.
5. **Compare side-by-side** with the RedHat baseline (51/66 = 77.3%):
   - Pass / fail / soft-pass deltas
   - Latency distribution
   - Any new failure modes
6. **Decide** the production profile name `qwen3.6-35b-a3b-nvfp4-nvidia`:
   - NVIDIA weights win on quality → keep nvidia/ source
   - Tie or RedHat wins → keep RedHat source, rename profile to reflect that
7. **Document** in `V9-35B-A3B-NVFP4-NVIDIA-RECIPE.md` under a new "v9.1 results" section.

### Success criteria (loose)
- Boot succeeds (proves PR #42124 fix works on our build)
- Smoke pass rate within ±5 pp of RedHat baseline (anything better is a bonus)

### Rollback
v9.1 image stays; profile reverts to RedHat weights.

### Estimated effort
~1 hour boot + smoke run, plus comparison writeup.

---

## Phase 5 — Streaming tool dispatch

**Goal**: enable end-to-end SSE streaming for tool calls. Both vLLM (≥ v0.21) and our stack must consume it.

### Per the prior research
- vLLM 0.22 already has the upstream primitives (PRs #40700 streaming with `required`, #41110 named tool_choice).
- Our stack does NOT consume them:
  - `manyforge/scripts/proxy/vllm-proxy.py:422-429` buffers SSE responses
  - `openclaw_assistant_bridge/adapter.py:754` sets `"stream": False`
  - `manyforge_assistant_bridge/bridge.py:1216-1223` parses tool_calls from full buffered response

### Implementation steps
1. **Proxy passthrough mode**: add `OPENCLAW_PROXY_STREAM_PASSTHROUGH=true` env. When set, the proxy forwards SSE chunks as they arrive (no buffering); JSONL log captures the full reassembled response after the stream closes (for debugging continuity).
2. **Direct-lane bridge SSE support**: flip `"stream": True` on the vLLM call. Add an SSE event reader that:
   - Emits partial tool-call deltas as they arrive
   - Reassembles the complete tool-call list before validating + executing
3. **OpenClaw-lane bridge SSE support**: same treatment for the gateway call. NOTE: openclaw gateway may itself need streaming support — investigate first.
4. **Composer compatibility check**: if composer expects buffered responses, gate behind `OPENCLAW_ASSISTANT_STREAM_TO_COMPOSER=true` env so the bridge can keep buffering at the composer boundary even if it streams from vLLM.

### Validation
- **A/B smoke runs**: same corpus, streaming ON vs OFF. Pass rate should be unchanged (correctness must not regress); latency to first tool-call dispatch should drop meaningfully.
- **JSONL diff**: stream-reassembled response equals buffered response for the same prompt + seed.

### Success criteria
- Smoke pass rate stream-ON within ±1 pp of stream-OFF
- First-tool-call latency reduced by ≥30% on long-thinking cases (one of our biggest pain points — 6 fails timed out at >150s)

### Estimated effort
~4-6 hours implementation + 2 hours validation.

---

## Phase 6 — Final comparison matrix

Run the canonical comparison once all upstream phases land:

| Lane | Model | Image | Streaming | Recipe | Pass rate |
|---|---|---|---|---|---|
| OpenClaw | cosmos-reason2-8b | v9 (current) | OFF | iter-32 (baseline) | TBD |
| OpenClaw | cosmos-reason2-8b | v9 | OFF | post-Phase-2 fix | TBD |
| Direct | cosmos-reason2-8b | v9 | OFF | post-Phase-1 history | TBD |
| Direct | cosmos-reason2-8b | v9 | ON | post-Phase-1+5 | TBD |
| OpenClaw | Qwen3.6-35B-A3B-NVFP4 (RedHat) | v9 | OFF | iter-3 (current 77.3%) | 51/66 ✓ |
| OpenClaw | Qwen3.6-35B-A3B-NVFP4 (NVIDIA) | v9.1 | OFF | NVIDIA Spark | TBD |
| OpenClaw | Qwen3.6-35B-A3B-NVFP4 (NVIDIA) | v9.1 | ON | Spark + streaming | TBD |
| Direct | Qwen3.6-35B-A3B-NVFP4 (best) | v9.1 | ON | post-Phase-1+5 | TBD |

Picks the production combo for each (model, scenario) pair.

---

## Execution order (dependencies)

```
Phase 0 (DONE)
  ↓
Phase 1 — direct lane history ────────────────────┐
Phase 2 — cosmos regression (uses existing data)  │  (these two can run in parallel — different
Phase 3 — v9.1 image build ──┐                    │   files, different GPU windows)
                              ↓                   │
                            Phase 4 — NVIDIA 35B test on v9.1
                                                  ↓
                              Phase 5 — streaming tool fixes ←┘
                                          ↓
                                        Phase 6 — final matrix
```

Critical path: Phase 3 (build) → Phase 4 (NVIDIA test). All others are flexible.

---

## Risks and mitigations

| Risk | Likelihood | Mitigation |
|---|---|---|
| Phase 3 image build breaks something unrelated | Medium | Tag v9.1 as additive; keep v9 image; smoke-regress before adoption |
| Direct-lane history changes break OpenClaw lane | Low | Code paths separate; OpenClaw lane only touched if needed; feature-flag both |
| Cosmos regression has no fix on v9 | Medium | If neither template/parser/transformers swap recovers, document the regression as a model-incompatibility on v9, recommend keeping cosmos pinned to v8 image until cause is understood upstream |
| Streaming changes destabilize tool-call parsing | Medium | Gate behind env flag; A/B smoke verification before flipping default |
| GPU time budget (Thor is the only test bench) | Medium | Phase 1 (CPU/code work) and Phase 2 (JSONL inspection) front-loaded; v9.1 build can run overnight |

---

## What I need from you before executing

1. **Approve the plan** — any phase you want re-scoped, dropped, or re-ordered?
2. **Approve commit policy**: per `feedback_commits_explicit_only` I will not commit without explicit per-step authorization. Expected pattern: I implement → present diff → you approve → I commit. Confirm.
3. **Phase 1 design decisions** (above table) — defaults are fine for me; flag any you want different.

Once approved, I'll start Phase 1 + Phase 2a in parallel (they don't share GPU).
